# S0-TriMask-HS v0.2 -- 结果

硬前向 + 直通梯度的文本门，并加入文本稀疏项（λ_sparse_T = 0.2）。唯一正式运行恰好 500 次 optimizer update；实现提交 `e67862ba8adaa93b5c133a303ec763c9af47539d`。

## 训练

- 恰好 500 次更新；LR horizon 3651（三 epoch 计划，未被压缩），每 epoch 1217 步

- 总墙钟 520.6 秒，平均 0.9085 秒/步，峰值显存 24.481 GiB（4 卡 × 256 对，global batch 1024，未做累积）

- 目标：10·L1 + 1·L2 + 1·L3 + 2·L_sparse_I + 0.2·L_sparse_T；共同初始化 SHA256 `caf61198def1b78654d6b70ef16db9a3475ea81b3a16ab8a799989f574de2faf`

- checkpoint `46f6d9c7e31ce3298d08e87a32b4228ca3bf5e216a674079deb5b0fa9fc0c4ef`；裸学生 `c9ad482c3df3a2628eeb2985f259e09ec2efec065bd9b75ab050fca9ace40987`


## 原生 CLS/EOS 检索（R@1/5/10）

| 模型 | COCO I2T | COCO T2I | Urban I2T | Urban T2I |
|---|---|---|---|---|
| S0@500（门槛） | 0.6058/0.8220/0.8906 | 0.4124/0.6709/0.7662 | 0.8700/0.9710/0.9880 | 0.8420/0.9670/0.9810 |
| S0_TriMask@500（soft, v0.1） | 0.6028/0.8224/0.8902 | 0.4144/0.6720/0.7664 | 0.8710/0.9750/0.9870 | 0.8370/0.9620/0.9830 |
| **S0_TriMask_HS@500** | 0.6006/0.8240/0.8870 | 0.4121/0.6710/0.7660 | 0.8740/0.9750/0.9890 | 0.8370/0.9630/0.9820 |

R@1 差值（百分点）：相对 S0 COCO 图→文 -0.52、文→图 -0.03；相对 soft TriMask COCO 图→文 -0.22、文→图 -0.24。Urban 相对 S0 图→文 +0.40、文→图 -0.50。

## 晋级门

COCO I2T R@1 >= 0.60580 and COCO T2I R@1 >= 0.41236, at least one strictly higher (raw full precision)

实测 COCO I2T R@1 = 0.60060（未达标）、COCO T2I R@1 = 0.41208（未达标）→ **FAIL**


## 三路与稀疏项

| step | L1 | L2 | L3 | 2·L_sparse_I | 0.2·L_sparse_T | loss_total | adv_gap | 第三路不劣比例 |
|---|---|---|---|---|---|---|---|---|
| 1 | 4.9904 | 1.3052 | 4.9904 | 0.8884 | 0.2000 | 57.2881 | 1.8792 | 0.057 |
| 20 | 0.7302 | 0.7055 | 0.7298 | 1.8522 | 0.1921 | 10.7819 | 0.0862 | 0.352 |
| 100 | 0.6850 | 0.9789 | 0.9979 | 1.7959 | 0.1942 | 10.8171 | 0.2362 | 0.291 |
| 250 | 0.4897 | 0.5304 | 0.5698 | 1.7124 | 0.1895 | 7.8989 | 0.0843 | 0.270 |
| 500 | 0.3251 | 0.3253 | 0.3466 | 1.5897 | 0.1823 | 5.6948 | 0.0704 | 0.318 |

## 文本硬门与两侧 mask

| step | mT 保留率 | mT 为 0 的比例 | mT 全关样本 | pT 均值 | pT min/max | mT 空/全开样本 | mI 保留率 | 交集数量 | Jaccard | 交集为空比例 |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 1.0000 | 0.0000 | 0.0000 | 0.8889 | 0.889/0.8889 | 0.0000/1.0000 | 0.4442 | 227.4 | 0.4442 | 0.0000 |
| 20 | 0.9606 | 0.0394 | 0.0000 | 0.9221 | 0.151/0.9988 | 0.0000/0.0000 | 0.9261 | 465.1 | 0.9284 | 0.0000 |
| 100 | 0.9712 | 0.0288 | 0.0000 | 0.9348 | 0.027/0.9999 | 0.0000/0.0000 | 0.8979 | 454.5 | 0.9043 | 0.0000 |
| 250 | 0.9477 | 0.0523 | 0.0000 | 0.8790 | 0.000/0.9999 | 0.0000/0.0000 | 0.8562 | 427.1 | 0.8603 | 0.0000 |
| 500 | 0.9117 | 0.0883 | 0.0000 | 0.8288 | 0.001/1.0000 | 0.0000/0.0000 | 0.7949 | 380.1 | 0.7700 | 0.0000 |

末步：mT 保留率 0.9117、其中为 0 的比例 0.0883、全关样本比例 0.0000；pT 均值 0.8288（min 0.0012 / max 1.0000，距阈值 0.05 内的比例 0.0449）；mT 与未 mask 文本的余弦均值 0.95805；文本方向改变比例 1.0000；两侧 mask 交集均值 380.1、并集为空比例 0.0000。


注意：`adv_gap` 是 relu(ℓ3 − best_single) 的**正部平均**，不是有正负抵消的平均差；两个 margin（最强负例 vs 负例 logsumexp）含义不同，报告与日志中都分开命名。


## 评估协议

COCO canonical：`phase30a_fixed_cohort_eval.py --canonical --canonical_only --coco`，全量 val2017 5000 图 × 25000 文本；runner 在启动前显式检查 `COCO_DATA_ROOT` 与注释文件绝对路径。Urban-1k：`eval_urban1k_cls.py`，1000 图 / 1000 文本，`--expect-steps 500` 已断言。两个评估的 checkpoint_sha256 都等于导出裸学生的 SHA256。

仅使用 normalize(encode_image(I)) 与 normalize(encode_text(C))；没有 mask reranking、三路分数融合、教师或 decoder。


## 运行过程与可复现性

- 第一次启动失败：runner 当时从 PATH 解析到 `/usr/local/bin/torchrun`（python3.11，没有 ftfy），训练脚本以错误解释器启动立刻报错。runner 保留真实退出码、把状态写成 `failed` 并**没有**继续评估不存在的 checkpoint；修复为“取 `--python` 同目录的 torchrun”后重新启动，训练恰好 500 次更新。

- 评估阶段用修复后的 runner 又跑了一次（`--phases export,coco,urban`）：两次的指标逐位相同，唯一不同的字段是写进 JSON 的 `git_head`（`d002086` → `74073f4`），所以文件哈希不同但**指标可复现**。

- `run_status.json` 由 runner 原子写入（临时文件 + `os.replace`），包含 phase、各阶段真实退出码、checkpoint 校验结果与最终结论；本次尝试记在 `attempt`，上一次完成的尝试保留在 `attempt_history`。

- 只读面板与训练完全解耦：它在 127.0.0.1:8765 提供服务，不 import torch、不加载 checkpoint、不占 GPU，训练跑完后面板继续以只读方式展示完成结果。


## 归因边界与限制

- 本轮**同时**把文本门从软重加权换成硬前向+直通梯度，并加入了 λ_sparse_T = 0.2，因此结果只能先归因为这套组合，不能拆成单项因果结论。

- 硬门产生 0 不等于删掉了正确的语义；稀疏度提高不等于检索提高。

- 没有证明单个 512 维坐标对应任何情感或概念；没有证明两个 mask 承担了不同语义。

- 单 seed、单配置、500 步，未做 250 步选型，也未延长到 3 epoch。


## NOT RUN

- 没有 hard-only 对照（本轮同时改了门型和加入了文本稀疏项）
- 没有优势 loss（lambda_adv = 0），第三路优势只做诊断
- 没有第四路未 mask 全局对齐，没有交叉池化、U 或重建
- 没有固定文本门对照臂，无法把差异单独归因于硬门或稀疏项
- 没有 250 步选型、没有超参搜索、没有延长到 3 epoch